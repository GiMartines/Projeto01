#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#define MAX 100
 int main(){

 char vendedores[10];
 int perc[10], quantVen, i, k, vendas;

 for(i=0; i<10; i++){
    printf("Digite o nome do vendedor %i: ", i);
    fflush (stdin);
    scanf("%d", &vendedores[i]);
    printf("Digite a quantidade de vendas desse vendedor: ");
    fflush (stdin);
    scanf("%i", &quantVen);
 }


 }
